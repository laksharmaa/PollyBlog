import { useState } from "react";
import Icon from "../common/Icon";
import { api } from "../../services/api";
import { stripHtml } from "../../utils/text";
export default function ListenPanel({blog,isAuthenticated}){ const [voice,setVoice]=useState("Matthew"); const [audio,setAudio]=useState(null); const [speaking,setSpeaking]=useState(false); const [error,setError]=useState(""); const narrate=async()=>{if(!isAuthenticated)return window.location.assign("/login");setSpeaking(true);setError("");try{const d=await api("/api/speech",{method:"POST",body:JSON.stringify({text:stripHtml(blog.blogContent),voiceId:voice,blogId:blog.blogId})});setAudio(d.audioUrl)}catch(e){setError(e.message)}finally{setSpeaking(false)}}; return <aside className="listen-panel"><div className="listen-label"><Icon name="volume"/> Narrate this story</div><p>Let a synthetic voice read the words back to you.</p><select value={voice} onChange={e=>setVoice(e.target.value)}><option>Matthew</option><option>Joanna</option><option>Kendra</option><option>Ivy</option></select><button className="button dark-button full" onClick={narrate} disabled={speaking}><Icon name={speaking?"pause":"play"}/>{speaking?"Generating…":"Listen"}</button>{!isAuthenticated&&<small>Sign in to use Narrate.</small>}{error&&<p className="form-error">{error}</p>}{audio&&<audio controls autoPlay src={audio}/>}</aside>; }
