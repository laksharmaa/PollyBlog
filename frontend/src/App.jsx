import {BrowserRouter} from "react-router-dom";import AppRoutes from "./routes/AppRoutes";import {useAuth} from "./hooks/useAuth";import {useTheme} from "./hooks/useTheme";export default function App(){const{isAuthenticated,signIn,signOut}=useAuth();const{dark,toggleTheme}=useTheme();return <BrowserRouter><AppRoutes isAuthenticated={isAuthenticated} onLogin={signIn} onLogout={signOut} dark={dark} onToggleTheme={toggleTheme}/></BrowserRouter>}
