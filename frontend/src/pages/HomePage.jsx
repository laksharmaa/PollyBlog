import {useEffect,useState} from "react";import {api} from "../services/api";import EmptyState from "../components/common/EmptyState";import StoryGrid from "../components/blog/StoryGrid";export default function HomePage(){const[blogs,setBlogs]=useState([]);const[loading,setLoading]=useState(true);const[error,setError]=useState("");useEffect(()=>{api("/api/public-blogs").then(d=>setBlogs(Array.isArray(d)?d:[])).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);return <div><section className="hero"><div className="hero-grid"><div><p className="eyebrow">A quieter kind of blog</p><h1>Human thoughts,<br/><em>machine voice.</em></h1><p className="hero-copy">Write what you mean. Publish it simply. Then hear your words come alive through a synthetic voice.</p><div className="hero-actions"><a className="button primary" href="/create-blog">Start writing <span>→</span></a><a className="text-link" href="#stories">Read stories</a></div></div><div className="hero-art"><div className="signal">{[30,75,130,175,115,62,25].map((h,i)=><span key={i} style={{height:h}} className={i===3?"accent-signal":""}/>)}</div><div className="hero-note"><span>01</span><p>Every story<br/>deserves a voice.</p></div></div></div></section><section id="stories" className="section"><div className="section-heading"><div><p className="eyebrow">From the community</p><h2>Latest stories</h2></div><span className="muted">{blogs.length} published</span></div>{error?<EmptyState title="Couldn’t load the stories" text={error}/>:blogs.length||loading?<StoryGrid blogs={blogs} loading={loading}/>:<EmptyState title="The page is quiet." text="Be the first person to publish something." action="Write a story"/>}</section></div>}
