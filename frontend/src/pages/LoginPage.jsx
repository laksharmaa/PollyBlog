import {useState} from "react";import {useNavigate} from "react-router-dom";import AuthPage from "../components/auth/AuthPage";import AuthField from "../components/auth/AuthField";import {api} from "../services/api";import Icon from "../components/common/Icon";export default function LoginPage({onLogin}){const nav=useNavigate();const[u,setU]=useState("");const[p,setP]=useState("");const[e,setE]=useState("");const[l,setL]=useState(false);const submit=async x=>{x.preventDefault();setL(true);setE("");try{const d=await api("/login",{method:"POST",body:JSON.stringify({username:u,password:p})});onLogin(d);nav("/")}catch(err){setE(err.message)}finally{setL(false)}};return <AuthPage eyebrow="Welcome back" title="Continue reading." subtitle="Your library and drafts are waiting."><form className="auth-form" onSubmit={submit}><AuthField label="Username" value={u} onChange={setU} autoComplete="username"/><AuthField label="Password" type="password" value={p} onChange={setP} autoComplete="current-password"/><p className="auth-switch"><a href="/forgot-password">Forgot your password?</a> · <a href="/resend-verification">Resend verification</a></p>{e&&<p className="form-error">{e}</p>}<button className="button primary full" disabled={l}>{l?"Signing in…":"Sign in"}<Icon name="arrow"/></button><p className="auth-switch">New here? <a href="/register">Create an account</a></p></form></AuthPage>}
